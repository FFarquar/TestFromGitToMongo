{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Debug Blazor WASM",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "webRoot": "${workspaceFolder}"

        }
    ]
}